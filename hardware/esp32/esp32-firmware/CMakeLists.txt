# ============================================================
# ESP32-S3 二次元 AI 角色硬件客户端 — 顶层 CMakeLists.txt
# 框架: ESP-IDF v5.0+
# ============================================================
cmake_minimum_required(VERSION 3.16)

# 导入 ESP-IDF 构建系统
include($ENV{IDF_PATH}/tools/cmake/project.cmake)

project(esp32-ai-character)
