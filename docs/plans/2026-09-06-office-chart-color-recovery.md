# PPT 图表改色与失败恢复

状态：已实现并验证（2026-09-06）。

## 已复现问题

- 图表颜色的未知 patch 字段被忽略，却返回成功；同一文件内存重放证明图表 XML 未改变。
- inspect 将稳定元素 ID 传给仅按内部 ID 查找的函数，图表内容返回 null。
- Office 结构化失败未进入通用错误统计，重复检查和猜测参数无法及时结束。
- 中断恢复只保留最后一轮工具结果，后续复盘丢失前面实际执行记录。
- 原图表修改只写 archive，实时渲染模型未同步；保存重开正确不能证明当前画面已改变。

## 实施与验收

1. 图表默认文字、标题、轴标签、轴标题、图例和数据标签颜色的原生 XML 写入、解析、渲染和重开验证。
2. 严格验证修改字段，修复稳定 ID 查询，提供简单改色操作与准确 Skill 示例；无变化不推进文档版本。
3. 识别结构化失败并按编辑目标累计无进展尝试，同时保留读取/恢复能力；不影响人工编辑触发的正常版本冲突。
4. 累积未完成 turn 的已完成消息；停止后按顺序恢复且不重复，保留错误与有效文本。
5. 真实图表场景、原生文件重开、指定目标以外内容保留，以及 Python/TypeScript 定向回归和编辑器构建。

不覆盖用户当前演示文稿，真实文件只用于内存重放或隔离副本检查。

## 验收结果

- 实际故障文件隔离回放：`s_4/e_88d1be81` 经一次 `setChart` 改色，19 个渲染文字项均为白色；数据不变，保存前后仅 `ppt/charts/chart1.xml` 改变，其它页面和图表保持原样。
- 浏览器调用真实 `SlideCanvas` 检查修改前、即时修改后、保存重开后三个画面。即时刷新曾暴露模型未同步问题，已补上重新解析并增加保存前 inspect/render tree 断言。
- 后端相关测试 140 项通过，覆盖结构化失败、正常版本冲突、按目标限制无进展重试、用户注入后重置、串行/并行批次中断、多轮恢复与无变化回执。
- 编辑器 Vitest 45 项与入口契约测试通过；宿主 OfficeEditorHost 6 项通过。
- Office 编辑器和 WebUI 类型检查、Python Ruff、mona-pptx Skill 校验、桌面编辑器资源构建通过。
- 这次验证覆盖原生命令链和真实画面，不将其等同于新一轮自然语言模型端到端测试。正在运行的 dev 后端需要重启以载入 Runner/Skill 改动，编辑器需重新打开载入新资源。
