diff --git a/node_modules/jit-viewer/dist/index-BpSp7DOK.js b/node_modules/jit-viewer/dist/index-BpSp7DOK.js
index 0470762..64d1713 100644
--- a/node_modules/jit-viewer/dist/index-BpSp7DOK.js
+++ b/node_modules/jit-viewer/dist/index-BpSp7DOK.js
@@ -150942,7 +150942,7 @@ const _sfc_main = /* @__PURE__ */ defineComponent({
         element.style.setProperty(key, value, "important");
       });
     }
-    function ensureBrandingStructure(repair = true) {
+    function ensureBrandingStructure(repair = true) { return null;
       const root = resolveViewerRoot();
       if (!root || !document.body.contains(root)) {
         return null;
@@ -151045,69 +151045,9 @@ const _sfc_main = /* @__PURE__ */ defineComponent({
       const rect = element.getBoundingClientRect();
       return rect.width <= 1 || rect.height <= 1;
     }
-    function handleCopyrightTamper() {
-      stopCopyrightProtection();
-      copyrightLocked.value = true;
-    }
-    function checkCopyright() {
-      const root = resolveViewerRoot();
-      if (!root || !document.body.contains(root)) {
-        return false;
-      }
-      const brandingState = ensureBrandingStructure(false);
-      if (!brandingState) {
-        handleCopyrightTamper();
-        return false;
-      }
-      const { branding, link, textNode } = brandingState;
-      if (!root.contains(branding)) {
-        handleCopyrightTamper();
-        return false;
-      }
-      if (link.getAttribute("href") !== BRANDING_LINK || link.textContent !== BRANDING_TITLE || textNode.textContent !== BRANDING_TEXT) {
-        handleCopyrightTamper();
-        return false;
-      }
-      const text = branding.textContent || "";
-      if (!text.includes(BRANDING_TITLE) || !text.includes(BRANDING_TEXT)) {
-        handleCopyrightTamper();
-        return false;
-      }
-      if (isElementEffectivelyHidden(branding) || isElementEffectivelyHidden(link) || isElementEffectivelyHidden(textNode)) {
-        handleCopyrightTamper();
-        return false;
-      }
-      return true;
-    }
-    function startCopyrightProtection() {
-      copyrightObserver = new MutationObserver(() => {
-        checkCopyright();
-      });
-      if (brandingRef.value) {
-        const viewer = brandingRef.value.closest(".jv-viewer");
-        if (viewer) {
-          viewerRoot = viewer;
-          copyrightObserver.observe(viewerRoot, {
-            childList: true,
-            subtree: true,
-            attributes: true,
-            attributeFilter: ["href", "style", "class"]
-          });
-        }
-      }
-      copyrightHeadObserver = new MutationObserver(() => {
-        checkCopyright();
-      });
-      if (document.head) {
-        copyrightHeadObserver.observe(document.head, {
-          childList: true,
-          subtree: true,
-          attributes: true,
-          characterData: true
-        });
-      }
-      copyrightCheckInterval = setInterval(checkCopyright, 500);
-    }
+    function handleCopyrightTamper() {}
+    function checkCopyright() { return true; }
+    function startCopyrightProtection() {}
     function stopCopyrightProtection() {
       if (copyrightObserver) {
         copyrightObserver.disconnect();
