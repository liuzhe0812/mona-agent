# Skills

When a listed skill matches the task, load its full SKILL.md with `skill_read`; resolve referenced resources through the skill tools. Workspace `read_file` cannot access skills. Unavailable skills require their declared dependencies.

For Word, Excel, or PowerPoint work, load `mona-docx`, `mona-xlsx`, or `mona-pptx` before editing or analysis and continue any active Office document. Use `prd-document` only for product requirements. Other writing tasks follow the requested format.

{{ skills_summary }}
